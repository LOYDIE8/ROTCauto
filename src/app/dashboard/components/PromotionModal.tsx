'use client';

import React from 'react';

/**
 * Placeholder component for the promotion evaluation and execution modal.
 */
export default function PromotionModal() {
  return (
    <button className="w-full bg-black/50 border border-gray-700 text-gray-500 font-mono text-xs uppercase py-2 px-4 opacity-50 cursor-not-allowed">
      [ Evaluate Promotion Eligibility ]
    </button>
  );
}
