'use client';

import React from 'react';

/**
 * Placeholder component for the event creation modal accessible by Officers.
 */
export default function CreateEventModal() {
  return (
    <button className="bg-[var(--neon-cyan)] text-black font-mono font-bold uppercase py-2 px-4 hover:bg-cyan-400 transition-colors opacity-50 cursor-not-allowed">
      [ Schedule Event ]
    </button>
  );
}
